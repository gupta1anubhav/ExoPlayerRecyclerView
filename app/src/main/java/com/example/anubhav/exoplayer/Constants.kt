package com.example.anubhav.exoplayer

class Constants {
    companion object {

        const val MIN_BUFFER_DURATION = 1000

        const val MAX_BUFFER_DURATION = 5000

        const val MIN_PLAYBACK_START_BUFFER = 1000

        const val MIN_PLAYBACK_RESUME_BUFFER = 3000
    }
}
